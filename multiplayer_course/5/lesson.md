# Урок 5. UI и сцены: меню и старт игры

## Цель
- Разделить меню и игру на сцены.
- Использовать UI‑кнопки для готовности.
- Переключать сцены по сетевому событию `start`.

## Требования
- Python 3.7+
- spritePro доступен в проекте

## Теория (кратко)
- Сцены позволяют разделять логику меню и игры.
- UI в меню не должен мешать логике игры.
- Сетевые события удобны для синхронного старта.
- В `SpritePro 3.x` scene-based игру лучше запускать через `s.run(...)`, а не через ручной `while True`.

## EventBus и прямые сообщения
- EventBus — для игровых событий (shoot, ready, start), удобно иметь подписчиков.
- Прямые `net.send()`/`send_every()` — для частых пакетов (позиции), меньше оверхеда.
- EventBus не заменяет сетевой слой, это просто удобная надстройка.

## Практика
1. Запустите `example_menu_scenes.py`.
1. Нажмите кнопку Ready в каждом окне.
1. Убедитесь, что сцена меняется после старта.

## Современный запуск

Для таких уроков текущий рекомендуемый паттерн такой:

- `s.run(..., multiplayer=True)` используется как единая точка входа
- при желании можно оставить `multiplayer_main(net, role)` и передать его через `multiplayer_entry=...`
- внутри `multiplayer_main(...)` инициализируется контекст
- дальше сцены и игровой цикл запускаются через обычный `s.run(setup=...)` или напрямую через `scene=...`

## Вопросы для самопроверки
- Почему меню лучше вынести в отдельную сцену?
- Что произойдет, если сцену сменить внутри `update` без `return`?
- Где удобнее хранить `net` для сцен?

## Задания
- В `practice_menu_scenes.py` допишите переключение Ready.
- Обновляйте текст кнопки при смене состояния.
- Запустите `start` только после готовности обоих игроков.

## Решение
- См. `solution_menu_scenes.py`.

## Что еще изучить
- Интерполяция и сглаживание движения.
- Авторитетный сервер и базовая валидация.
