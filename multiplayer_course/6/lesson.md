# Урок 6. Мини‑игра: очки за зону

## Цель
- Отправлять игровые события (набрал очки).
- Хранить общий счет у хоста.
- Синхронизировать счет через сетевые сообщения.

## Требования
- Python 3.7+
- spritePro доступен в проекте

## Теория (кратко)
- Состояние (счет) удобно хранить у хоста.
- Клиенты посылают события, хост рассылает итог.
- Нужен анти‑спам: небольшой кулдаун между событиями.

## EventBus и прямые сообщения
- EventBus — для игровых событий (shoot, ready, start), удобно иметь подписчиков.
- Прямые `net.send()`/`send_every()` — для частых пакетов (позиции), меньше оверхеда.
- EventBus не заменяет сетевой слой, это просто удобная надстройка.

## Практика
1. Запустите `example_score_zone.py`.
1. Подведите игрока к центру — очки должны расти.
1. Убедитесь, что счет одинаков у обоих клиентов.

## Вопросы для самопроверки
- Почему счет хранит хост?
- Что случится без кулдауна?
- Чем отличается `score` и `score_update`?

## Задания
- В `practice_score_zone.py` добавьте отправку `score`.
- Хост должен увеличивать счет и рассылать `score_update`.
- Добавьте кулдаун для событий.
- (Вариант) Оформите приём/обработку событий `score` и `score_update` через `s.events.connect` и проброс входящих сообщений в `s.events.send` (по аналогии с уроком 3, example_ready_state_events.py).

## Решение
- См. `solution_score_zone.py`.

## Что еще изучить
- Интерполяция и сглаживание движения.
- Авторитетный сервер и базовая валидация.
