# Шаблон урока

## Цель
- ...

## Требования
- Python 3.7+
- spritePro установлен и доступен в проекте

## Теория (кратко)
- ...

## Уровень урока
- Это low-level урок про сетевые примитивы или app-level урок про готовую игру?
- Если в уроке используются сцены и полноценный игровой flow, предпочитайте `s.run(...)`.
- Если урок показывает транспортный слой (`NetClient`, `NetServer`, `ctx.send/poll`), ручной цикл допустим как учебный low-level вариант.

## EventBus и прямые сообщения
- EventBus — для игровых событий (shoot, ready, start), удобно иметь подписчиков.
- Прямые `net.send()`/`send_every()` — для частых пакетов (позиции), меньше оверхеда.
- EventBus не заменяет сетевой слой, это просто удобная надстройка.

## Практика
1. ...
1. ...

## Рекомендуемый запуск в `SpritePro 3.x`
- `s.networking.run(...)` — поднимает хост/клиентов и вызывает `multiplayer_main(net, role)`.
- `s.run(...)` — запускает уже саму игру/сцены без ручного `while True`.
- Если урок про кастомное лобби, отдельно поясните разницу между low-level решением и встроенным `s.networking.run(use_lobby=True)`.

## Вопросы для самопроверки
- ...
- ...

## Задания
- ...
- ...

## Решение
- См. `solution_*.py` в папке урока.

## Что еще изучить
- ...
